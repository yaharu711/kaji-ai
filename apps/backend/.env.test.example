POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=kaji_ai_test
# ローカルPostgresを使う場合は上記3つと DATABASE_URL のホストを適宜変更してください。
# Neon や他のマネージドDBを使う場合は POSTGRES_* 系は省略しても構いません。
DATABASE_URL=postgresql://USER:PASSWORD@YOUR-NEON-PROJECT.neon.tech/kaji_ai?sslmode=require
# docker-entrypoint.sh が起動時に必ず参照するため必須。
TEST_DATABASE_URL=postgresql://USER:PASSWORD@YOUR-NEON-PROJECT.neon.tech/kaji_ai_test?sslmode=require
